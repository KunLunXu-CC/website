extend type Query{
  getTag: [Tag]
}

extend type Mutation{
  createTag(body: CreateTagInput!): [Tag]
}

type Tag{
  id: ID
  name: String
  color: String
  icon: String
  parent: Tag
  creator: String
  creationTime: Date
  updater: String
  updateTime: Date
  status: Int
}

input CreateTagInput{
  name: String!
  parent: ID
  color: String
  icon: String
}

input UpdateTag{
  name: String
  parent: ID
  color: String
  icon: String
}
