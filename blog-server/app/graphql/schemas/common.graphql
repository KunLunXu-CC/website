type Query{
  # 占位符 
  _empty: String
}

type Mutation{
  # 占位符 
  _empty:String
}

scalar Date

type Stats{
  # 统计
  total: Int
}

type Page{
  # 查询返回分页信息
  page: Int,
  pageSize: Int,
}

input PageInput{
  # 查询输入分页信息
  page: Int!
  pageSize: Int!
}
