import React, { useRef } from 'react';
import { Modal } from 'qyrc';

const Note = (props) => {
  return (
    <div style={{ background: '#04071b' }}>
      笔记
    </div>
  );
}

export default Note;
