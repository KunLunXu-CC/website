#### react页面之间传参的几个方式：
##### 1.1 路由 log/:id
- 固定

##### 2.2 location.search 传参
login?id=ad12324s2342242f

##### 2.3 this.props.location.state 实现带参传值
补充： hash路由无法设置 state
带参数页面跳转
this.props.history.push("/targetURL"， {passParam: true});
第一个参数为你页面跳转时，需要传递的参数，如果不需要传参数，则为null。
第二个参数为跳转目标页面。
目标页面获取参数
let temp = this.props.location.state;

#### 将 location.search 部分转换为对象
```js
/**
 * 将 location.search 部分转换为对象
 * @param {String} search url中查询字符串
 * 在react-router中可通过 this.props.location.search 获取 location.search
 */
export const searchToObject = (search) => {
  if(!search) return null;
  search = search.substr(1);
  const arr = search.split("&");
  const obj = {};
  let newArr = [];
  arr.map((value,index,arr) => {
    newArr = value.split("=");
    if(newArr[0] && newArr[1]) {
      obj[newArr[0]] = unescape(newArr[1]);
    }
  })
  return obj
}

```
#### 在项目中查询块的合理封装
##### 1.1
在当前项目中查询块只是作为木偶组件一样的角色，对表单进行布局和控制；
在父级组件中编写查询逻辑（获取列表）通过 props 传给查询块；
在父组件初始化时加载列表，在


#### React children 遍历以及克隆的使用
##### 1.1 
```js
// 遍历 children
React.Children.map(children, child => {});

// 克隆 Elemeng
React.cloneElement(
  Element, 
  {
    //追加新的 props
  }, 
  [
    Element, // 如果没有则完全修改了 Element
    // new Element 
  ]
);

React.Children.map(this.props.children, child =>{
  return React.cloneElement(child, {
    size: this.props.size,
    parentState: this.state,
    age: 123
  }, [child, <p>追加新的child<p>])
});
```
#### 对象属性存在性判断
```js
// 对象 updateBody 是否有 payoutWay 属性
updateBody.hasownproperty('payoutWay')
```